{
  "name": "life-cashflow-simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check input-wizard.js && node --check calculations.js && node --check csv.js && node --check storage.js"
  }
}
